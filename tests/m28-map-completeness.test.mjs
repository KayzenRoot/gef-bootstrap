import test from'node:test';import assert from'node:assert/strict';import{createHash}from'node:crypto';import{buildTestMap}from'../packages/test-impact-engine/dist/public.js';const H=s=>'sha256:'+createHash('sha256').update(s).digest('hex');const O={digest:{algorithm:'sha256',digest:s=>createHash('sha256').update(s).digest('hex')}};test('missing source dependency makes map explicitly incomplete',()=>{const r=buildTestMap([{id:'a',fingerprint:H('a'),dependsOn:['missing']}],[],O);assert.equal(r.value.complete,false)});
