import test from'node:test';import assert from'node:assert/strict';import{createHash}from'node:crypto';import{createTestImpactResult}from'../packages/test-impact-engine/dist/public.js';const H=s=>'sha256:'+createHash('sha256').update(s).digest('hex');const O={digest:{algorithm:'sha256',digest:s=>createHash('sha256').update(s).digest('hex')}};test('result digest binds unresolved obligations',()=>{const s={tests:['t'],level:'L2',uncertainty:'NONE',reasons:[],digest:H('s')};const a=createTestImpactResult({selection:s,unresolved:[]},O).value,b=createTestImpactResult({selection:s,unresolved:['x']},O).value;assert.notEqual(a.digest,b.digest)});
