import test from'node:test';import assert from'node:assert/strict';import{platformRelevant}from'../packages/test-impact-engine/dist/public.js';test('platform relevance is conservative and explicit',()=>{assert.equal(platformRelevant({id:'a',fingerprint:'x',sources:[]},'linux'),true);assert.equal(platformRelevant({id:'b',fingerprint:'x',sources:[],platforms:['windows']},'linux'),false)});
