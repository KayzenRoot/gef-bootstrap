import test from'node:test';import assert from'node:assert/strict';import{failureScopedRetest}from'../packages/test-impact-engine/dist/public.js';test('failure scoped retest preserves broad mandatory remainder',()=>{const p={tests:['a','b','c'],level:'L4',uncertainty:'NONE',reasons:[],digest:'x'};const r=failureScopedRetest(p,[{testId:'b',fingerprint:'f'}],['c']);assert.deepEqual(r.firstWave,['b','c']);assert.deepEqual(r.mandatoryRemainder,['a','b','c'])});
