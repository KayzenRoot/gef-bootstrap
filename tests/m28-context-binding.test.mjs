import test from'node:test';import assert from'node:assert/strict';import{createHash}from'node:crypto';import{bindImpactContext,sameImpactContext}from'../packages/test-impact-engine/dist/context-binding.js';const H=s=>'sha256:'+createHash('sha256').update(s).digest('hex');const O={digest:{algorithm:'sha256',digest:s=>createHash('sha256').update(s).digest('hex')}};const C={projectId:'p',lineageId:'l',candidateDigest:H('c'),policyDigest:H('p'),profileDigest:H('pr'),runtimeDigest:H('r'),toolchainDigest:H('t'),platform:'linux'};test('context receipt is deterministic and cross-candidate splice fails equality',()=>{const a=bindImpactContext(C,O),b=bindImpactContext({...C},O);assert.equal(a.value.digest,b.value.digest);assert.equal(sameImpactContext(C,{...C,candidateDigest:H('other')}),false)});
