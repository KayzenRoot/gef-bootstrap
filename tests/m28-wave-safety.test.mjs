import test from'node:test';import assert from'node:assert/strict';import{createHash}from'node:crypto';import{specifyValidationWaves,concurrencyEnvelope}from'../packages/test-impact-engine/dist/public.js';const O={digest:{algorithm:'sha256',digest:s=>createHash('sha256').update(s).digest('hex')}};test('wave specification canonicalizes tests and retains constraints',()=>{const r=specifyValidationWaves([{tests:['z','a','z'],after:['pre'],exclusiveResources:['db']}],O);assert.deepEqual(r.value[0].tests,['a','z']);assert.deepEqual(r.value[0].after,['pre']);assert.deepEqual(r.value[0].exclusiveResources,['db'])});test('concurrency envelope reports conflicting resource declarations for same test',()=>{assert.equal(concurrencyEnvelope([{tests:['a'],resource:'db'},{tests:['a'],resource:'fs'}]).safe,false)});
