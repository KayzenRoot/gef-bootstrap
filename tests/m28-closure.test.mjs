import test from'node:test';import assert from'node:assert/strict';import{createHash}from'node:crypto';import{buildTestMap,selectImpactedTests}from'../packages/test-impact-engine/dist/public.js';const H=s=>'sha256:'+createHash('sha256').update(s).digest('hex');const O={digest:{algorithm:'sha256',digest:s=>createHash('sha256').update(s).digest('hex')}};test('source dependency closure reaches downstream tests',()=>{const m=buildTestMap([{id:'a',fingerprint:H('a')},{id:'b',fingerprint:H('b'),dependsOn:['a']},{id:'c',fingerprint:H('c'),dependsOn:['b']}],[{id:'tc',fingerprint:H('tc'),sources:['c']}],O).value;const p=selectImpactedTests(m,['a'],'linux',{requiredLevel:'L2',policyDigest:H('p'),profileDigest:H('q')},O).value;assert.deepEqual(p.tests,['tc'])});
