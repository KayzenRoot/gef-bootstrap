name: M19 Project Registry
on:
  pull_request:
    paths:
      - 'packages/project-registry/**'
      - 'tests/m19-*.mjs'
      - '.engineering/work-orders/GBS-WO-M19-001.md'
      - '.engineering/GBS-WO-M19-ADMISSION.md'
      - 'planning/modules/area-e-continuity/m19-project-registry/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/m19-platform.yml'
permissions:
  contents: read
jobs:
  focused:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npm audit --audit-level=low
      - run: npm run typecheck
      - run: node --test tests/m19-project-registry.test.mjs tests/m19-hardening.test.mjs tests/m19-startup-purity.test.mjs
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npm audit --audit-level=low
      - run: npm test
