import type{CompatibilityDecision,CompatibilityRequirement,HumanResponseProjection,MachineResponseEnvelope,MetricOwnershipMatrix,NextNecessaryActionCapsule,OperationOptions,ResponseContractCapsule,ResponseIntegrityReceipt,ResponseBindingObservation,Result,StaleResponseReport,DelegatedMetricClaim,BlockerProjection,StateConflictWitness,VerdictDecision}from'./types.js';
import{cancelled,compareCodePoint,deepFreeze,fail,isSha256,sha,sortedUnique,validId}from'./utils.js';
import{buildResponseProvenanceIndex,verifyResponseContractCapsule,verifyResponseSchemaEnvelope,verifySourceBoundFieldClaim}from'./s01-envelope.js';
import{verifyDelegatedMetricClaim}from'./s02-metrics.js';
import{deriveResponseVerdict,projectApplicableBlockers,verifyBlockerProjection,verifyNextNecessaryActionCapsule,verifyStateConflictWitness}from'./s03-verdict.js';
import{assessResponseRedaction,assessResponseSize,buildMinimumSufficientResponseFields}from'./s04-compactness.js';
const verdictRank:Record<string,number>={SUCCESS:0,PARTIAL:1,INDETERMINATE:2,BLOCKED:3,CONFLICT:4,RECOVERY_REQUIRED:5};
function metricSort(a:DelegatedMetricClaim,b:DelegatedMetricClaim){return compareCodePoint(a.metricKey,b.metricKey)||compareCodePoint(a.claimDigest,b.claimDigest);}
function blockerSort(a:BlockerProjection,b:BlockerProjection){return compareCodePoint(a.blockerId,b.blockerId)||compareCodePoint(a.blockerDigest,b.blockerDigest);}
function conflictSort(a:StateConflictWitness,b:StateConflictWitness){return compareCodePoint(a.subject,b.subject)||compareCodePoint(a.witnessDigest,b.witnessDigest);}
function verdictDigestValid(v:VerdictDecision,options:OperationOptions):Result<boolean>{const d=sha(options,{verdict:v.verdict,reasons:[...v.reasons].sort(compareCodePoint)});if(!d.ok)return d;return{ok:true,value:d.value===v.verdictDigest};}
export interface BuildMachineResponseInput{readonly capsule:ResponseContractCapsule;readonly metrics:readonly DelegatedMetricClaim[];readonly blockers:readonly BlockerProjection[];readonly conflicts:readonly StateConflictWitness[];readonly nextAction:NextNecessaryActionCapsule;readonly requiredFields:readonly string[];readonly partial?:boolean|undefined;readonly indeterminate?:boolean|undefined;readonly requestedVerdict?:VerdictDecision['verdict']|undefined;}
export function buildMachineResponseEnvelope(input:BuildMachineResponseInput,matrix:MetricOwnershipMatrix,options:OperationOptions):Result<MachineResponseEnvelope>{const c=cancelled(options);if(c)return c;const cv=verifyResponseContractCapsule(input.capsule,options);if(!cv.ok)return cv;if(!cv.value)return fail('RESPONSE_CAPSULE_TAMPERED','Machine response cannot be built from an invalid capsule');const fields=buildMinimumSufficientResponseFields(input.capsule.sourceClaims,input.requiredFields,options);if(!fields.ok)return fields;const metrics=[...input.metrics].sort(metricSort);for(const m of metrics){const v=verifyDelegatedMetricClaim(m,matrix,options);if(!v.ok)return v;if(!v.value)return fail('METRIC_CLAIM_TAMPERED','Machine response contains an invalid metric claim',m.metricKey);}const projected=projectApplicableBlockers(input.blockers,options);if(!projected.ok)return projected;const blockers=[...projected.value].sort(blockerSort);const conflicts=[...input.conflicts].sort(conflictSort);for(const w of conflicts){const v=verifyStateConflictWitness(w,options);if(!v.ok)return v;if(!v.value)return fail('CONFLICT_WITNESS_TAMPERED','Machine response contains an invalid conflict witness',w.subject);}const nv=verifyNextNecessaryActionCapsule(input.nextAction,options);if(!nv.ok)return nv;if(!nv.value)return fail('NEXT_ACTION_TAMPERED','Machine response next-action capsule is invalid');const redaction=assessResponseRedaction(fields,blockers.map(b=>b.message),options);if(!redaction.ok)return redaction;if(!redaction.value.safe)return fail('RESPONSE_REDACTION_REQUIRED','Portable response contains secret-like or private locator material',redaction.value.rejectedFields.join(','));let requested=input.requestedVerdict;if(fields.some(f=>f.freshness==='CONFLICT'))requested='CONFLICT';else if(fields.some(f=>f.freshness==='STALE'||f.freshness==='UNKNOWN')&&requested===undefined)requested='INDETERMINATE';const verdict=deriveResponseVerdict(blockers,conflicts,{partial:input.partial,indeterminate:input.indeterminate,requestedVerdict:requested},options);if(!verdict.ok)return verdict;const pi=buildResponseProvenanceIndex(fields,options);if(!pi.ok)return pi;const semanticBody={schemaDigest:input.capsule.schema.schemaDigest,projectId:input.capsule.projectId,responseKind:input.capsule.responseKind,checkpointBindingDigest:input.capsule.checkpointBindingDigest,resumeBindingDigest:input.capsule.resumeBindingDigest,registryBindingDigest:input.capsule.registryBindingDigest,fields,metrics,blockers,conflicts,nextAction:input.nextAction,verdict:verdict.value,provenanceIndex:pi.value};const size=assessResponseSize(fields,metrics,blockers,pi.value.bindings.reduce((n,b)=>n+b.claimDigests.length,0),semanticBody,options);if(!size.ok)return size;if(!size.value.withinLimits)return fail('EXPANSION_REFERENCE_REQUIRED','Response exceeds configured semantic size limits');const sd=sha(options,semanticBody);if(!sd.ok)return sd;const md=sha(options,{...semanticBody,semanticDigest:sd.value});if(!md.ok)return md;return{ok:true,value:deepFreeze({schema:input.capsule.schema,projectId:input.capsule.projectId,responseKind:input.capsule.responseKind,checkpointBindingDigest:input.capsule.checkpointBindingDigest,resumeBindingDigest:input.capsule.resumeBindingDigest,registryBindingDigest:input.capsule.registryBindingDigest,fields,metrics,blockers,conflicts,nextAction:input.nextAction,verdict:verdict.value,provenanceIndex:pi.value,semanticDigest:sd.value,machineDigest:md.value})};}
export function verifyMachineResponseEnvelope(machine:MachineResponseEnvelope,matrix:MetricOwnershipMatrix,options:OperationOptions):Result<boolean>{const sv=verifyResponseSchemaEnvelope(machine.schema,options);if(!sv.ok)return sv;if(!sv.value||!validId(machine.projectId)||!validId(machine.responseKind)||![machine.checkpointBindingDigest,machine.resumeBindingDigest,machine.registryBindingDigest,machine.semanticDigest,machine.machineDigest].every(isSha256))return{ok:true,value:false};for(const f of machine.fields){const v=verifySourceBoundFieldClaim(f,options);if(!v.ok)return v;if(!v.value)return{ok:true,value:false};}for(const m of machine.metrics){const v=verifyDelegatedMetricClaim(m,matrix,options);if(!v.ok)return v;if(!v.value)return{ok:true,value:false};}for(const b of machine.blockers){const v=verifyBlockerProjection(b,options);if(!v.ok)return v;if(!v.value)return{ok:true,value:false};}for(const w of machine.conflicts){const v=verifyStateConflictWitness(w,options);if(!v.ok)return v;if(!v.value)return{ok:true,value:false};}const nv=verifyNextNecessaryActionCapsule(machine.nextAction,options);if(!nv.ok||!nv.value)return nv.ok?{ok:true,value:false}:nv;const vv=verdictDigestValid(machine.verdict,options);if(!vv.ok||!vv.value)return vv.ok?{ok:true,value:false}:vv;const minimum=deriveResponseVerdict(machine.blockers,machine.conflicts,{},options);if(!minimum.ok)return minimum;if((verdictRank[machine.verdict.verdict]??-1)<(verdictRank[minimum.value.verdict]??0))return{ok:true,value:false};const pi=buildResponseProvenanceIndex(machine.fields,options);if(!pi.ok)return pi;if(pi.value.indexDigest!==machine.provenanceIndex.indexDigest)return{ok:true,value:false};const semanticBody={schemaDigest:machine.schema.schemaDigest,projectId:machine.projectId,responseKind:machine.responseKind,checkpointBindingDigest:machine.checkpointBindingDigest,resumeBindingDigest:machine.resumeBindingDigest,registryBindingDigest:machine.registryBindingDigest,fields:machine.fields,metrics:machine.metrics,blockers:machine.blockers,conflicts:machine.conflicts,nextAction:machine.nextAction,verdict:machine.verdict,provenanceIndex:machine.provenanceIndex};const sd=sha(options,semanticBody);if(!sd.ok)return sd;if(sd.value!==machine.semanticDigest)return{ok:true,value:false};const md=sha(options,{...semanticBody,semanticDigest:sd.value});if(!md.ok)return md;return{ok:true,value:md.value===machine.machineDigest};}
function metricLine(m:DelegatedMetricClaim){if(m.availability!=='AVAILABLE')return`metric.${m.metricKey}=${m.availability}`;if(m.rangeValue)return`metric.${m.metricKey}=${m.rangeValue.min}..${m.rangeValue.max}${m.unit?` ${m.unit}`:''}`;return`metric.${m.metricKey}=${String(m.scalarValue)}${m.unit?` ${m.unit}`:''}`;}
export function buildHumanResponseProjection(machine:MachineResponseEnvelope,matrix:MetricOwnershipMatrix,options:OperationOptions):Result<HumanResponseProjection>{const mv=verifyMachineResponseEnvelope(machine,matrix,options);if(!mv.ok)return mv;if(!mv.value)return fail('MACHINE_RESPONSE_TAMPERED','Human projection requires a verified machine response');const lines:string[]=[];for(const f of machine.fields)lines.push(`${f.field}=${f.value}`);lines.push(`verdict=${machine.verdict.verdict}`);for(const b of machine.blockers)lines.push(`blocker.${b.blockerId}=${b.message}`);for(const m of machine.metrics)lines.push(metricLine(m));lines.push(`next_action=${machine.nextAction.state==='KNOWN'?machine.nextAction.actionRef:machine.nextAction.state}`);const red=assessResponseRedaction(machine.fields,lines,options);if(!red.ok)return red;if(!red.value.safe)return fail('HUMAN_PROJECTION_REDACTION_REQUIRED','Human response would expose secret/private material',red.value.rejectedFields.join(','));const claims=sortedUnique([...machine.fields.map(f=>f.claimDigest),...machine.metrics.map(m=>m.claimDigest),...machine.blockers.map(b=>b.blockerDigest),machine.nextAction.capsuleDigest,machine.verdict.verdictDigest]);const body={semanticDigest:machine.semanticDigest,lines,materialClaimDigests:claims};const d=sha(options,body);if(!d.ok)return d;return{ok:true,value:deepFreeze({...body,projectionDigest:d.value})};}
export function verifyHumanResponseProjection(human:HumanResponseProjection,machine:MachineResponseEnvelope,matrix:MetricOwnershipMatrix,options:OperationOptions):Result<boolean>{const rebuilt=buildHumanResponseProjection(machine,matrix,options);if(!rebuilt.ok)return rebuilt;return{ok:true,value:human.semanticDigest===machine.semanticDigest&&human.projectionDigest===rebuilt.value.projectionDigest&&JSON.stringify(human.lines)===JSON.stringify(rebuilt.value.lines)&&JSON.stringify(human.materialClaimDigests)===JSON.stringify(rebuilt.value.materialClaimDigests)};}
export function buildResponseIntegrityReceipt(machine:MachineResponseEnvelope,human:HumanResponseProjection|null,matrix:MetricOwnershipMatrix,options:OperationOptions):Result<ResponseIntegrityReceipt>{const mv=verifyMachineResponseEnvelope(machine,matrix,options);if(!mv.ok)return mv;let valid=mv.value;if(human){const hv=verifyHumanResponseProjection(human,machine,matrix,options);if(!hv.ok)return hv;valid=valid&&hv.value;}const body={schemaDigest:machine.schema.schemaDigest,semanticDigest:machine.semanticDigest,machineDigest:machine.machineDigest,humanProjectionDigest:human?.projectionDigest??null,sourceClaimDigests:machine.fields.map(f=>f.claimDigest).sort(compareCodePoint),metricClaimDigests:machine.metrics.map(m=>m.claimDigest).sort(compareCodePoint),blockerDigests:machine.blockers.map(b=>b.blockerDigest).sort(compareCodePoint),valid};const d=sha(options,body);if(!d.ok)return d;return{ok:true,value:deepFreeze({...body,receiptDigest:d.value})};}
export function checkResponseCompatibility(schema:MachineResponseEnvelope['schema'],requirement:CompatibilityRequirement,options:OperationOptions):Result<CompatibilityDecision>{const c=cancelled(options);if(c)return c;const reasons:string[]=[];if(!requirement.supportedSchemaVersions.includes(schema.schemaVersion))reasons.push('UNSUPPORTED_SCHEMA_VERSION');if(!requirement.supportedProfiles.includes(schema.profile))reasons.push('UNSUPPORTED_PROFILE');const caps=new Set(requirement.capabilities);const missing=schema.requiredCapabilities.filter(c=>!caps.has(c)).sort(compareCodePoint);if(missing.length)reasons.push('MISSING_CAPABILITY');const body={compatible:reasons.length===0,missingCapabilities:missing,reasons:sortedUnique(reasons)};const d=sha(options,body);if(!d.ok)return d;return{ok:true,value:deepFreeze({...body,decisionDigest:d.value})};}
export function detectStaleResponse(machine:MachineResponseEnvelope,observation:ResponseBindingObservation,options:OperationOptions):Result<StaleResponseReport>{const c=cancelled(options);if(c)return c;const stale:string[]=[];if(observation.checkpointBindingDigest!==machine.checkpointBindingDigest)stale.push('checkpoint');if(observation.resumeBindingDigest!==machine.resumeBindingDigest)stale.push('resume');if(observation.registryBindingDigest!==machine.registryBindingDigest)stale.push('registry');if(machine.fields.length){if(!observation.fieldValidityBindings){stale.push(...machine.fields.map(f=>`field:${f.field}`));}else for(const f of machine.fields)if(observation.fieldValidityBindings[f.field]!==f.validityBindingDigest)stale.push(`field:${f.field}`);}if(machine.metrics.length){if(!observation.metricValidityBindings){stale.push(...machine.metrics.map(m=>`metric:${m.metricKey}`));}else for(const m of machine.metrics)if(observation.metricValidityBindings[m.metricKey]!==m.validityBindingDigest)stale.push(`metric:${m.metricKey}`);}const normalized=sortedUnique(stale);const body={stale:normalized.length>0,staleSubjects:normalized};const d=sha(options,body);if(!d.ok)return d;return{ok:true,value:deepFreeze({...body,reportDigest:d.value})};}
