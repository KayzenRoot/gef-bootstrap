import type{DeltaChangeInventory,HedsAuthorityBoundary,HedsIntentCapsule,HedsIntentInput,OperationOptions,Result,ReviewSourceInput,ReviewSourceProjection,SemanticDeltaEntry,SemanticStateIdentity,CrossLineageWitness}from'./types.js';
import{allSha,compareCodePoint,deepFreeze,digestValue,fail,Guard,ok,sortedUnique,validId}from'./utils.js';
function uniqueValid(values:readonly string[]){return values.length===new Set(values).size&&values.every(validId);}
export function createHedsIntent(input:HedsIntentInput,options:OperationOptions):Result<HedsIntentCapsule>{if(![input.intentId,input.projectId,input.lineageId,input.baselineId,input.candidateId].every(validId)||!validId(input.purpose))return fail('HIC26_ID_INVALID','HEDS intent identifiers are invalid.');if(!allSha([input.baselineIdentityDigest,input.candidateIdentityDigest,input.reviewPolicyDigest])||!uniqueValid(input.requestedSubjectIds))return fail('HIC26_BINDING_INVALID','HEDS intent semantic identities, policy or requested subjects are invalid.');const body={intentId:input.intentId,projectId:input.projectId,lineageId:input.lineageId,baselineId:input.baselineId,candidateId:input.candidateId,baselineIdentityDigest:input.baselineIdentityDigest,candidateIdentityDigest:input.candidateIdentityDigest,reviewPolicyDigest:input.reviewPolicyDigest,requestedSubjectIds:sortedUnique(input.requestedSubjectIds),purpose:input.purpose};const d=digestValue(options,'HIC26',body);return d.ok?ok(deepFreeze({...body,intentDigest:d.value})):d;}
export function createHedsAuthorityBoundary(intent:HedsIntentCapsule,options:OperationOptions):Result<HedsAuthorityBoundary>{const body={intentDigest:intent.intentDigest,authority:'SEMANTIC_DELTA_REVIEW_ONLY' as const,mayValidateEvidence:false as const,mayDecideProof:false as const,mayDecideAssurance:false as const,maySelectTests:false as const,mayOperateGit:false as const,mayPromoteCheckpoint:false as const,mayCalculateProgress:false as const,mayComputeProjectStatus:false as const};const d=digestValue(options,'HAB26',body);return d.ok?ok(deepFreeze({...body,boundaryDigest:d.value})):d;}
function createStateIdentity(stateId:string,projectId:string,lineageId:string,coverageDigest:string,sourceProjectionDigests:readonly string[],options:OperationOptions):Result<SemanticStateIdentity>{if(![stateId,projectId,lineageId].every(validId)||!allSha([coverageDigest,...sourceProjectionDigests]))return fail('SSI26_INPUT_INVALID','Semantic state identity inputs are invalid.');const semanticBody={projectId,lineageId,coverageDigest,sourceProjectionDigests:sortedUnique(sourceProjectionDigests)};const d=digestValue(options,'SSI26_STATE',semanticBody);return d.ok?ok(deepFreeze({stateId,projectId,lineageId,coverageDigest,semanticDigest:d.value})):d;}
export function createBaselineSemanticIdentity(stateId:string,projectId:string,lineageId:string,coverageDigest:string,sourceProjectionDigests:readonly string[],options:OperationOptions){return createStateIdentity(stateId,projectId,lineageId,coverageDigest,sourceProjectionDigests,options);}
export function createCandidateSemanticIdentity(stateId:string,projectId:string,lineageId:string,coverageDigest:string,sourceProjectionDigests:readonly string[],options:OperationOptions){return createStateIdentity(stateId,projectId,lineageId,coverageDigest,sourceProjectionDigests,options);}
export function createSemanticStateIdentity(stateId:string,projectId:string,lineageId:string,coverageDigest:string,sourceProjectionDigests:readonly string[],options:OperationOptions){return createStateIdentity(stateId,projectId,lineageId,coverageDigest,sourceProjectionDigests,options);}
export function sealReviewSourceProjection(input:ReviewSourceInput,options:OperationOptions):Result<ReviewSourceProjection>{if(![input.subjectId,input.sourceId,input.ownerId,input.projectId,input.lineageId].every(validId))return fail('RSP26_ID_INVALID','Review source identifiers are invalid.',input.subjectId);if(!allSha([input.semanticDigest,input.validityBindingDigest,...input.dependencyDigests]))return fail('RSP26_DIGEST_INVALID','Review source semantic or validity bindings are invalid.',input.subjectId);const body={subjectId:input.subjectId,sourceId:input.sourceId,ownerId:input.ownerId,projectId:input.projectId,lineageId:input.lineageId,semanticDigest:input.semanticDigest,validityBindingDigest:input.validityBindingDigest,dependencyDigests:sortedUnique(input.dependencyDigests)};const d=digestValue(options,'RSP26',body);return d.ok?ok(deepFreeze({...body,projectionDigest:d.value})):d;}
export function createCrossLineageWitness(baseline:SemanticStateIdentity,candidate:SemanticStateIdentity,options:OperationOptions):Result<CrossLineageWitness>{const body={projectMatch:baseline.projectId===candidate.projectId,lineageMatch:baseline.lineageId===candidate.lineageId},comparisonAdmissible=body.projectMatch&&body.lineageMatch;const d=digestValue(options,'XLG26',{...body,comparisonAdmissible,baseline:baseline.semanticDigest,candidate:candidate.semanticDigest});return d.ok?ok(deepFreeze({...body,comparisonAdmissible,witnessDigest:d.value})):d;}
function mapSources(items:readonly ReviewSourceProjection[],side:string,guard:Guard):Result<Map<string,ReviewSourceProjection>>{const map=new Map<string,ReviewSourceProjection>();for(const item of items){const step=guard.step(`${side}:${item.subjectId}`);if(!step.ok)return step;const existing=map.get(item.subjectId);if(existing)return fail('DCI26_DUPLICATE_SUBJECT',`Duplicate ${side} subject projection.`,item.subjectId);map.set(item.subjectId,item);}return ok(map);}
function symmetric(a:readonly string[],b:readonly string[]){const aa=new Set(a),bb=new Set(b);return sortedUnique([...a.filter(x=>!bb.has(x)),...b.filter(x=>!aa.has(x))]);}
function deltaEntry(subjectId:string,before:ReviewSourceProjection|undefined,after:ReviewSourceProjection|undefined,coverageComplete:boolean,options:OperationOptions):Result<SemanticDeltaEntry>{if(before&&!after&&!coverageComplete)return fail('DCI26_REMOVAL_UNPROVEN','Subject absence cannot be classified as REMOVED without complete comparison coverage.',subjectId);const semanticChanged=before===undefined||after===undefined||before.semanticDigest!==after.semanticDigest,authorityChanged=before===undefined||after===undefined||before.ownerId!==after.ownerId||before.sourceId!==after.sourceId,validityChanged=before===undefined||after===undefined||before.validityBindingDigest!==after.validityBindingDigest,dependencyChanged=symmetric(before?.dependencyDigests??[],after?.dependencyDigests??[]),changed=new Set(dependencyChanged);if(semanticChanged){if(before)changed.add(before.semanticDigest);if(after)changed.add(after.semanticDigest);}if(validityChanged){if(before)changed.add(before.validityBindingDigest);if(after)changed.add(after.validityBindingDigest);}const projectionChanged=semanticChanged||authorityChanged||validityChanged||dependencyChanged.length>0;if(projectionChanged){if(before)changed.add(before.projectionDigest);if(after)changed.add(after.projectionDigest);}const changedDependencyDigests=sortedUnique([...changed]);let changeClass:'ADDED'|'REMOVED'|'MODIFIED'|'UNCHANGED';if(!before&&after)changeClass='ADDED';else if(before&&!after)changeClass='REMOVED';else if(projectionChanged)changeClass='MODIFIED';else changeClass='UNCHANGED';const body={subjectId,changeClass,beforeProjectionDigest:before?.projectionDigest??null,afterProjectionDigest:after?.projectionDigest??null,semanticChanged,authorityChanged,validityChanged,changedDependencyDigests};const d=digestValue(options,'SDL26',body);return d.ok?ok(deepFreeze({...body,deltaDigest:d.value})):d;}
export function createDeltaChangeInventory(baseline:SemanticStateIdentity,candidate:SemanticStateIdentity,baselineSources:readonly ReviewSourceProjection[],candidateSources:readonly ReviewSourceProjection[],coverageComplete:boolean,options:OperationOptions):Result<DeltaChangeInventory>{const lineage=createCrossLineageWitness(baseline,candidate,options);if(!lineage.ok)return lineage;if(!lineage.value.comparisonAdmissible)return fail('DCI26_LINEAGE_MISMATCH','Baseline and candidate are not in the same project lineage.');const guard=new Guard(options),bm=mapSources(baselineSources,'baseline',guard);if(!bm.ok)return bm;const cm=mapSources(candidateSources,'candidate',guard);if(!cm.ok)return cm;const ids=sortedUnique([...bm.value.keys(),...cm.value.keys()]),entries:SemanticDeltaEntry[]=[];for(const id of ids){const step=guard.step(`delta:${id}`);if(!step.ok)return step;const r=deltaEntry(id,bm.value.get(id),cm.value.get(id),coverageComplete,options);if(!r.ok)return r;entries.push(r.value);}entries.sort((a,b)=>compareCodePoint(a.subjectId,b.subjectId));const body={baselineIdentityDigest:baseline.semanticDigest,candidateIdentityDigest:candidate.semanticDigest,coverageComplete,entries};const d=digestValue(options,'DCI26',body);return d.ok?ok(deepFreeze({...body,inventoryDigest:d.value})):d;}
