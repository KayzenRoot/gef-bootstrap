# M52 S02 — Project
Status: FROZEN

**PDP52 Project Diagnostic Probe** validates repository identity, canonical files, checkpoint/schema consistency, dirty/conflict state, artifact/evidence references and configuration without mutating them. It detects stale/missing/inconsistent state and emits stable finding codes.

Acceptance: no mutation, checkpoint cross-check, broken-reference detection, dirty-state visibility, deterministic findings.