# M51 S03 — GitHub CLI
Status: FROZEN

**GHM51 GitHub Capability Matrix** separates CLI presence/version from authentication, repository permission and API capability. CLI is optional when connector/API capability exists. Authentication details are redacted; missing capability becomes CAPABILITY_GAP rather than fabricated availability.

Acceptance: CLI-optional paths, auth-safe reporting, capability/permission distinction, offline/absent behavior.