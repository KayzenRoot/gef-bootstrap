# M41 S03 — Optional Handoff
Status: FROZEN

UGAS is an accelerator, never a prerequisite. **UOH41** emits a read-only handoff receipt containing requested capability, selected provider contract, context digest, input digest, budget, cancellation lineage and expected artifact contract. **USA41** Safe Absence routes unavailable/indeterminate UGAS to the generic path without changing approved scope or assurance.

Results are accepted only through downstream evidence/integrity owners. M41 cannot self-approve generated artifacts, mutate checkpoints, widen scope, or claim progress. Provider errors are isolated and normalized.

Acceptance: optionality invariant; exact context binding; provider-failure isolation; generic fallback equivalence; no downstream authority leakage.